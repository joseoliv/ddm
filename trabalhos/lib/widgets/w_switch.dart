import 'package:flutter/material.dart';

class WSwitch extends StatelessWidget {
  const WSwitch({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Switch widget'));
  }
}
