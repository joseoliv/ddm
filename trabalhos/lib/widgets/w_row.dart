import 'package:flutter/material.dart';

class WRow extends StatelessWidget {
  const WRow({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Row widget'));
  }
}
