import 'package:flutter/material.dart';

class WColumn extends StatelessWidget {
  const WColumn({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Column widget'));
  }
}
