import 'package:flutter/material.dart';

/// create a stateless widget that just show an image
class WLoadassetimage extends StatelessWidget {
  const WLoadassetimage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a Load asset image widget')
    );
  }
}

