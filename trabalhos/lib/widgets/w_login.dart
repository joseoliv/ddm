import 'package:flutter/material.dart';

class WLogin extends StatelessWidget {
  const WLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Login widget'));
  }
}
