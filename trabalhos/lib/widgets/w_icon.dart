import 'package:flutter/material.dart';

class WIcon extends StatelessWidget {
  const WIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Icon widget'));
  }
}
