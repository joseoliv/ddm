import 'package:flutter/material.dart';

class WRadio extends StatelessWidget {
  const WRadio({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Radio widget'));
  }
}
