import 'package:flutter/material.dart';

class WForm extends StatelessWidget {
  const WForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Form widget'));
  }
}
