import 'package:flutter/material.dart';

/// create a stateless widget that just show an image
class WChip extends StatelessWidget {
  const WChip({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: 
         /// Chip widget
    );
  }
}

