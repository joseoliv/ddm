import 'package:flutter/material.dart';

class WIconButton extends StatelessWidget {
  const WIconButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a IconButton widget'));
  }
}
