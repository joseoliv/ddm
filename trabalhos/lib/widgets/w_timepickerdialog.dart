import 'package:flutter/material.dart';

class WTimePickerDialog extends StatelessWidget {
  const WTimePickerDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a TimePickerDialog widget'));
  }
}
