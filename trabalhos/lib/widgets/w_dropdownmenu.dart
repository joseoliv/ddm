import 'package:flutter/material.dart';

class WDropdownMenu extends StatelessWidget {
  const WDropdownMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a DropdownMenu widget'));
  }
}
