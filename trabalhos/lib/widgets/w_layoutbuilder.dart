import 'package:flutter/material.dart';

class WLayoutBuilder extends StatelessWidget {
  const WLayoutBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a LayoutBuilder widget'));
  }
}
