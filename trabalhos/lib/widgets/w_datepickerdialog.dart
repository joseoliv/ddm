import 'package:flutter/material.dart';

/// create a stateless widget that just show an image
class WDatePickerDialog extends StatelessWidget {
  const WDatePickerDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a DatePickerDialog widget')
    );
  }
}

