import 'package:flutter/material.dart';

class WLoadassettext extends StatelessWidget {
  const WLoadassettext({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Load asset text widget'));
  }
}
