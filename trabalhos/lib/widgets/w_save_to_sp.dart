import 'package:flutter/material.dart';

class WSavetoSP extends StatelessWidget {
  const WSavetoSP({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Save to SP widget'));
  }
}
