import 'package:flutter/material.dart';

/// create a stateless widget that just show an image
class WOutlinedButton extends StatelessWidget {
  const WOutlinedButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a OutlinedButton widget')
    );
  }
}

