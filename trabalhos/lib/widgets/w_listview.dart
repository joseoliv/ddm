import 'package:flutter/material.dart';

class WListView extends StatelessWidget {
  const WListView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a ListView widget'));
  }
}
