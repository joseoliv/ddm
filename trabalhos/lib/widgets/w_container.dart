import 'package:flutter/material.dart';

class WContainer extends StatelessWidget {
  const WContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Container widget'));
  }
}
