import 'package:flutter/material.dart';

/// create a stateless widget that just show an image
class WStackandPositioned extends StatelessWidget {
  const WStackandPositioned({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: 
         /// Stack and Positioned widget
    );
  }
}

