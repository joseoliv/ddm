import 'package:flutter/material.dart';

class WReeditor extends StatelessWidget {
  const WReeditor({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Re_editor widget'));
  }
}
