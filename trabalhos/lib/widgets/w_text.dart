import 'package:flutter/material.dart';

class WText extends StatelessWidget {
  const WText({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Text widget'));
  }
}
