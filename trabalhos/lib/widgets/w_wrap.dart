import 'package:flutter/material.dart';

class WWrap extends StatelessWidget {
  const WWrap({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a Wrap widget'));
  }
}
